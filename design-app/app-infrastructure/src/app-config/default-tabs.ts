import type { MultiTabsSetting } from "@rchitect-design/types";

const defaultTabs:MultiTabsSetting = {
  hidden: false,
  cache: false,
  // Turn on
  show: true,
  // Is it possible to drag and drop sorting tabs
  canDrag: true,
  // Turn on quick actions
  showQuick: true,
  // Whether to show the refresh button
  showRedo: true,
  // Whether to show the collapse button
  showFold: true,
}
export default defaultTabs